name: Post welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
  contents: read # added permission for checkout
jobs:
  welcome:
    name: Post welcome comment
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # added checkout step

